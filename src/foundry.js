import 'arrive'
import { initSmoothScroll } from './js/part/smooth-scroll'

document.addEventListener('DOMContentLoaded', (event) => {
	// Initialize Lenis for smooth scrolling
	initSmoothScroll()
})
